function tabAnimation(t, e, i, a, o) {
  const s = document.querySelectorAll(t),
    c = document.querySelector(e),
    n = {};
  (n.current = c),
    (c.style.display = "block"),
    (c.style.opacity = 1),
    s[0].classList.add(a),
    o && s[0].classList.add(o),
    s.forEach((t, c) => {
      t.addEventListener("click", () => {
        if ([...t.classList].includes(a)) return;
        const d = 0 === c ? e : e.concat(`-${c}`),
          r = document.querySelector(d);
        document.querySelector(i);
        n.current ? ((n.previous = n.current), (n.current = r)) : (n.previous = r),
          (n.previous.style.opacity = 0),
          setTimeout(() => {
            n.previous.style.display = "none";
          }, 350),
          setTimeout(() => {
            n.current.style.display = "block";
          }, 450),
          setTimeout(() => {
            n.current.style.opacity = 1;
          }, 500),
          s.forEach((e) => {
            o &&
              t.innerText !== e.innerText &&
              setTimeout(() => {
                e.classList.remove(o);
              }, 200),
              e.classList.remove(a);
          }),
          o && (t.classList.add(a), t.classList.add(o)),
          t.classList.add(a);
      });
    });
}
document.addEventListener("DOMContentLoaded", () => {
  const t = document.querySelectorAll(".side-tabs-maroon"),
    e = document.querySelectorAll(".side-tabs-white");
  t &&
    (t.forEach((e, i) => {
      if (t.length > 1)
        e.classList.add(`maroon-duplicated-${i}`), tabAnimation(`.maroon-duplicated-${i} .maroon-item`, `.maroon-duplicated-${i} .text`, `.maroon-duplicated-${i} .col-md-8`, "tab-active");
      else {
        if (1 !== t.length) return;
        tabAnimation(".side-tabs-maroon .maroon-item", ".side-tabs-maroon .text", ".side-tabs-maroon .col-md-8", "tab-active");
      }
    }),
    e &&
      e.forEach((t, i) => {
        if (e.length > 1)
          t.classList.add(`white-duplicated-${i}`),
            tabAnimation(`.white-duplicated-${i} .white-item`, `.white-duplicated-${i} .text`, `.white-duplicated-${i} .col-md-8`, "tab-active-maroon", "text-white");
        else {
          if (1 !== e.length) return;
          tabAnimation(".side-tabs-white .white-item", ".side-tabs-white .text", ".side-tabs-white .col-md-8", "tab-active-maroon", "text-white");
        }
      }));
});
