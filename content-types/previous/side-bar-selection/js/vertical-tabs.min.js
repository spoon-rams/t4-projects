function tabAnimation(t, e, i, a) {
  const s = document.querySelectorAll(t),
    o = document.querySelector(e),
    n = {};
  (n.current = o),
    (o.style.display = "block"),
    (o.style.opacity = 1),
    s[0].classList.add(i),
    a && s[0].classList.add(a),
    s.forEach((t, o) => {
      t.addEventListener("click", () => {
        if ([...t.classList].includes(i)) return;
        const c = 0 === o ? e : e.concat(`-${o}`),
          r = document.querySelector(c);
        n.current ? ((n.previous = n.current), (n.current = r)) : (n.previous = r),
          (n.previous.style.opacity = 0),
          setTimeout(() => {
            n.previous.style.display = "none";
          }, 350),
          setTimeout(() => {
            n.current.style.display = "block";
          }, 450),
          setTimeout(() => {
            n.current.style.opacity = 1;
          }, 500),
          s.forEach((e) => {
            a &&
              t.innerText !== e.innerText &&
              setTimeout(() => {
                e.classList.remove(a);
              }, 200),
              e.classList.remove(i);
          }),
          a && (t.classList.add(i), t.classList.add(a)),
          t.classList.add(i);
      });
    });
}
document.addEventListener("DOMContentLoaded", () => {
  const t = document.querySelectorAll(".side-tabs-maroon"),
    e = document.querySelectorAll(".side-tabs-white");
  t &&
    (t.forEach((e, i) => {
      if (t.length > 1) e.classList.add(`maroon-duplicated-${i}`), tabAnimation(`.maroon-duplicated-${i} .maroon-item`, `.maroon-duplicated-${i} .text`, "tab-active");
      else {
        if (1 !== t.length) return;
        tabAnimation(".side-tabs-maroon .maroon-item", ".side-tabs-maroon .text", "tab-active");
      }
    }),
    e &&
      e.forEach((t, i) => {
        if (e.length > 1) t.classList.add(`white-duplicated-${i}`), tabAnimation(`.white-duplicated-${i} .white-item`, `.white-duplicated-${i} .text`, "tab-active-maroon", "text-white");
        else {
          if (1 !== e.length) return;
          tabAnimation(".side-tabs-white .white-item", ".side-tabs-white .text", "tab-active-maroon", "text-white");
        }
      }));
});
